{% extends 'base.html.twig' %}

{% block title %}Edit Events{% endblock %}

{% block body %}
<div class="container">
    <div class="row myrow">
        <h1>Edit Events</h1>
    
        {{ include('events/_form.html.twig', {'button_label': 'Update'}) }}
    
        <a  class="btn btn-warning btl" href="{{ path('app_events_index') }}">Back to list</a>
    
        {{ include('events/_delete_form.html.twig') }}
    </div>
</div>
{% endblock %}
